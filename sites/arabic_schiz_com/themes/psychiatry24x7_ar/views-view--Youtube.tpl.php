<?php
// $Id: views-view.tpl.php,v 1.13.2.2 2010/03/25 20:25:28 merlinofchaos Exp $
/**
 * @file views-view.tpl.php
 * Main view template
 *
 * Variables available:
 * - $classes_array: An array of classes determined in
 *   template_preprocess_views_view(). Default classes are:
 *     .view
 *     .view-[css_name]
 *     .view-id-[view_name]
 *     .view-display-id-[display_name]
 *     .view-dom-id-[dom_id]
 * - $classes: A string version of $classes_array for use in the class attribute
 * - $css_name: A css-safe version of the view name.
 * - $css_class: The user-specified classes names, if any
 * - $header: The view header
 * - $footer: The view footer
 * - $rows: The results of the view query, if any
 * - $empty: The empty text to display if the view is empty
 * - $pager: The pager next/prev links to display, if any
 * - $exposed: Exposed widget form/info to display
 * - $feed_icon: Feed icon to display, if any
 * - $more: A link to view more, if any
 * - $admin_links: A rendered list of administrative links
 * - $admin_links_raw: A list of administrative links suitable for theme('links')
 *
 * @ingroup views_templates
 */
global $base_url;
?>
<div class="<?php print $classes; ?>">
  <?php if ($admin_links): ?>
    <div class="views-admin-links views-hide">
      <?php print $admin_links; ?>
    </div>
  <?php endif; ?>
  <?php if ($header): ?>
    <div class="view-header">
      <?php print $header; ?>
    </div>
  <?php endif; ?>

  <?php if ($exposed): ?>
    <div class="view-filters">
      <?php print $exposed; ?>
    </div>
  <?php endif; ?>

  <?php if ($attachment_before): ?>
    <div class="attachment attachment-before">
      <?php print $attachment_before; ?>
    </div>
  <?php endif; ?>

  <?php if ($rows): ?>
    <div class="view-content">
      <?php $rows = str_replace('Youtube Video:','',$rows);
     // echo $base_url;
      $imgPath = $base_url."/".path_to_theme()."/images/";
     // echo "Gopal".$imgPath;
     echo "<div id='youtube_container'>";
     echo "<img src = ".$imgPath."hd_checkout.gif></img>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<img

src = ".$imgPath."hd_youtube.gif></img>";
     echo "<br /><br />";
      print $rows;

      $youtube_query = db_query ("SELECT node.nid AS nid, node_data_field_youtube.field_youtube_value AS

node_youtube_value
FROM node node LEFT JOIN content_type_youtube node_data_field_youtube ON node.vid = node_data_field_youtube.vid
WHERE node.type IN ('youtube') AND (node.status <> 0)");
      $youtube_fetch_array = db_fetch_array($youtube_query);
       echo "</div>";

      echo "<div id='youtube_ftcontainer'><p>";
      echo "<a href ='http://www.youtube.com/watch?v=".$youtube_fetch_array["node_youtube_value"]."'

target='_blank'>".t('Visit our youtube page')."</a>";
      echo "</p></div>";

      ?>
    </div>
  <?php elseif ($empty): ?>
    <div class="view-empty">
      <?php print $empty; ?>
    </div>
  <?php endif; ?>

  <?php if ($pager): ?>
    <?php print $pager; ?>
  <?php endif; ?>

  <?php if ($attachment_after): ?>
    <div class="attachment attachment-after">
      <?php print $attachment_after; ?>
    </div>
  <?php endif; ?>

  <?php if ($more): ?>
    <?php print $more; ?>
  <?php endif; ?>

  <?php if ($footer): ?>
    <div class="view-footer">
      <?php print $footer; ?>
    </div>
  <?php endif; ?>

  <?php if ($feed_icon): ?>
    <div class="feed-icon">
      <?php print $feed_icon; ?>
    </div>
  <?php endif; ?>

</div> <?php /* class view */ ?>
